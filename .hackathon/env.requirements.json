{
  "schema_version": 1,
  "services": [
    {
      "service": "Terac",
      "required_for": "authorized live human-input proof",
      "variables": [
        {
          "name": "TERAC_API_KEY",
          "secret": true,
          "required": true
        }
      ]
    }
  ],
  "notes": [
    "No value belongs in Git.",
    "Optional sponsor integrations are not selected yet and therefore are not listed."
  ]
}

