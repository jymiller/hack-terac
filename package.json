{
  "name": "hack-terac",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node app/server.mjs",
    "dev": "node --env-file=.env --watch app/server.mjs",
    "db:push": "psql \"$DATABASE_URL\" -f db/schema.sql",
    "check:concept-lock": "node scripts/check-concept-lock.mjs",
    "test": "node --test tests/*.test.mjs"
  },
  "dependencies": {
    "express": "^5.1.0",
    "pg": "^8.16.3",
    "stripe": "^22.4.0"
  }
}
